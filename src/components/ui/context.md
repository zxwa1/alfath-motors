# UI Components Context
This folder contains all generic, reusable UI components built on top of Shadcn UI and Tailwind CSS.
These components should NOT contain any business logic or data fetching.
They must adhere to the Flat Architecture rules and be 100% responsive.
Animations here should be kept minimal and generic (like hover states). Cinematic animations belong in `blocks/`.
